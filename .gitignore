# Python backend
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Models downloaded locally (large binaries — do not commit)
backend/models/
*.gguf
*.bin
*.onnx

# llama-server prebuilt binaries (downloaded, not vendored — see docs/planning/PRD.md)
llama-server/

# Chrome extension build output (if a bundler is introduced later)
extension/dist/
extension/node_modules/

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# Raw eval source data (copyrighted YouTube audio/transcripts — do not push to a public
# repo). Derived eval outputs (data/*.jsonl) are fine to commit.
data/json/
data/wav/

# Run logs (transient, not reproducible artifacts)
*.log

# Local screen recordings used for manual debugging review (large binaries)
ref/
