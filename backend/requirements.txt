# Install torch + torchaudio separately first with a CUDA-enabled index, e.g.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# (only needed for backend/vad.py's torch.hub silero-vad model — its hubconf
# imports torchaudio even though our usage never touches it directly. The
# STT path via faster-whisper/ctranslate2 doesn't depend on torch at all.)
fastapi
uvicorn[standard]
faster-whisper
numpy
httpx
# backend/hallucination_gate.py — embedding-similarity "Bag of Hallucinations"
# gate (2026-08-19). Pulls in transformers/torch (torch already required
# above for vad.py).
sentence-transformers
# backend/stt/qwen3_asr_engine.py — active STT engine as of 2026-08-26
# (config.STT_ENGINE = "qwen3-asr"), see research/topic/
# 20260826_stt_model_survey_gpu_full/. AutoModelForMultimodalLM/
# Qwen3-ASR support needs a recent transformers; pin a floor rather than
# trust whatever sentence-transformers pulls in transitively.
transformers>=5.13
# backend/translation/prompts.py loads prompts.yaml — was already pulled in
# transitively (huggingface_hub etc.) but declared explicitly since it's now
# a direct dependency.
pyyaml
# backend/music_gate.py — Demucs (htdemucs) vocal separation ahead of pitch
# tracking, so background music/BGM under normal talking doesn't corrupt the
# singing-detection read (2026-08-25). Uses torch/torchaudio, already
# required above for vad.py.
# vanilla (2026-08-26): singing detection disabled, see
# docs/planning/IMPROVEMENT_BACKLOG.md M1 — uncomment to restore.
# demucs
